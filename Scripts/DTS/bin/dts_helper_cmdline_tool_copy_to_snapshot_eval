#!/usr/bin/perl

use strict;

use warnings;

use JSON::XS;

use Data::Dump qw(pp);

use Path::Class;
    
my $json_coder = JSON::XS->new->ascii->pretty->allow_nonref;


while(<>)
{
	chomp;

        $_ =~ s/^qml: //g;

	print( $_, "\n" );
}
